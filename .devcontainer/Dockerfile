# Use official devcontainer Node/TypeScript image
FROM mcr.microsoft.com/devcontainers/typescript-node:latest

# Ensure the working directory exists
RUN mkdir -p /workspaces/sort

# Switch to the "node" user (important!)
USER node

# Default command: keep container alive
CMD ["sleep", "infinity"]
